apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
  labels:
    app: {{ .Release.Name }}
type: Opaque
stringData:
  DATABASE_URL: {{ .Values.supabase.databaseUrl | quote }}
  DIRECT_URL: {{ .Values.supabase.directUrl | quote }}
